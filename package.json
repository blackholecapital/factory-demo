{
  "name": "factory-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp public/app.js dist/app.js && cp public/styles.css dist/styles.css"
  }
}
